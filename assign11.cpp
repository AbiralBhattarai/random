#include<stdio.h>
#include<conio.h>
void main(){
	int a,b,op;
	float ans;
	printf("enter two numbers: ");
	scanf("%d",&a,&b);
	printf("Enter the operator:")l
	fflush = (stdin);
	op = getchar();
	switch(op){
		case "+":
			ans = a + b
			break;
		case "-":
			ans = a - b
			break;
		case "*":
			ans = a * b
			break;
		case "/":
			ans = a / b
			break;
		case "%":
			ans = a %b
			break;
	}
}
